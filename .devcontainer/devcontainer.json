{
    "build": {
        "dockerfile": "Dockerfile",
        "context": "..",
        "args": {
          "BUILDKIT_INLINE_CACHE": "0"
        }
    },
    "runArgs": [
      "--network=host"
    ]
  }